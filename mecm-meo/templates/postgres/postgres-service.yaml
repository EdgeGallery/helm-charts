apiVersion: v1
kind: Service
metadata:
  name: mecm-postgres
  labels:
    app: mecm-postgres
spec:
  selector:
    app: mecm-postgres

  ports:
    - port: {{ .Values.expose.postgres.port }}
      targetPort: 5432
