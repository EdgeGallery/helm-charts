apiVersion: v1
kind: Service
metadata:
  name: mepm-postgres
  labels:
    app: mepm-postgres
spec:
  selector:
    app: mepm-postgres
  ports:
    - port: {{ .Values.expose.postgres.port }}
      targetPort: 5432
